services:
  wakemypc-rust-transmitter:
    container_name: wakemypc-rust-transmitter
    build:
      context: .
      dockerfile: ./compose/production/Dockerfile
    env_file:
      .env
    # Sample values
    # environment:
    #   - RUST_LOG=debug
    #   - TRANSMITTER_ID=test-1
    #   - AUTH_TOKEN=test-token
    restart: unless-stopped
    network_mode: "host"
